Prompt 1 — FR-01 Extract (PROMPT, senior-tester protocol)

Đây là đoạn spec + SRS của FR-01 (`POST /api/register`, body: name, email, password). Liệt kê parameters, ràng buộc, actor/role, status code, response schema. Chưa sinh test.

Bối cảnh: SUT EShop (Express + SQLite). SRS nêu: email hợp lệ và duy nhất; password tối thiểu 8 ký tự, có hoa, thường, số, và 1 ký tự trong `@$!%*?&`.
