Prompt 1 — FR-09 Extract (PROMPT)

Đây là đoạn spec + SRS của FR-09 POST /api/apply-coupon. Liệt kê parameters, ràng buộc C1–C5, actor/role, status code, response schema, công thức percent/fixed. Chưa sinh test.
