# Agent Skill design notes

Student must draw original diagram manually. Diagram must show: inputs (API spec, SRS, feature), parser/normalizer, four coverage planners (domain/state/security/schema), candidate generator, deduplicator/critic, human-review gate, exporters (Excel/Markdown/Postman), and feedback loop from audit/execution defects back to prompts/rules.

Diagram acceptance: arrows show data flow; human review is a hard gate; execution and bug evidence are downstream of approval; no AI image/diagram generation is used.

